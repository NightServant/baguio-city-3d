# PostgreSQL + PostGIS (matches docker-compose.yml)
DATABASE_URL="postgresql://baguio:baguio@localhost:5432/baguio?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# Mapbox GL access token (client-side). Replace with a real pk.* token.
NEXT_PUBLIC_MAPBOX_TOKEN="pk.REPLACE_WITH_YOUR_MAPBOX_TOKEN"
